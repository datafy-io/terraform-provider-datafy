---
page_title: "datafy_account Resource - datafy"
subcategory: ""
description: |-
  Manages a Datafy account. Accounts are the top-level organizational unit in Datafy, used to group and manage AWS resources, IAM roles, tokens, and autoscaling rules.
---

# datafy_account (Resource)

Manages a Datafy account. Accounts are the top-level organizational unit in Datafy, used to group and manage AWS resources, IAM roles, tokens, and autoscaling rules.

Each account is created as a child of your organization's parent account. After creating an account, you can associate AWS IAM roles (`datafy_role_arn`), generate access tokens (`datafy_token`), and configure autoscaling rules (`datafy_autoscaling_rule`).

## Example Usage

### Basic account

```terraform
resource "datafy_account" "production" {
  name = "production"
}
```

### Account with associated resources

```terraform
resource "datafy_account" "staging" {
  name = "staging"
}

resource "datafy_role_arn" "staging" {
  account_id = datafy_account.staging.id
  arn        = "arn:aws:iam::123456789012:role/DatafyStagingRole"
}

resource "datafy_token" "staging_ci" {
  account_id  = datafy_account.staging.id
  description = "CI/CD token for staging"
  role_ids    = ["admin"]
}
```

{{ .SchemaMarkdown | trimspace }}

## Import

Existing accounts can be imported using the account ID:

```shell
terraform import datafy_account.example 79c406c5-7b64-43f2-ba76-9b01e74e3d90
```
